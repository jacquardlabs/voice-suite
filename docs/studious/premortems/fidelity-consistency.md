# Pre-mortem — Dedup fidelity procedure, fix Strunk cross-skill mask

- Design doc: docs/design/fidelity-consistency.md
- Branch: epic/audit-fixes--fidelity-consistency
- SHA: 1c26e41
- Date: 2026-07-10

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Collapsing a generator's restated steps to a pointer silently deletes a register-specific line along with the generic prose (e.g. voice-email's "match greeting/sign-off to the actual inventory from email.md", voice-chat's injection note, voice-rewrite's scale detection). | Diff each generator's SKILL.md before/after; confirm every removed line is generic procedure text, not register-specific guidance. |
| 2 | technical | The pointer target drifts from the step numbers it cites — voice-profile's step 1 gains the "2–4 samples" figure, so a generator that points at "steps 2–6" may reference the wrong steps after any renumber. | Confirm every generator's pointer cites the same step range and that voice-profile's numbered steps match what the pointers reference. |
| 3 | technical | The converged "2–4" count re-forks — the dedup's durability depends on the four generators NOT restating the number; a later edit re-inlining a count into one generator silently reintroduces the divergence with nothing flagging it. | Grep the four generators' no-profile fallback step for any standalone sample numeral ("2–3", "2–4", "a few"); there should be none — only register-flavor phrasing plus the pointer. |
| 4 | product | voice-email's new longform.md read primes formal emails toward doc/blog rhythm — longform.md mixes "docs, long emails, posts", so a post-heavy longform.md pulls an external email draft toward blog cadence, the opposite of the JTBD (reads as their email voice). | At acceptance, generate a long formal email against a profile whose longform.md is post-dominated; check the draft doesn't adopt doc-register rhythm and that email.md's own exemplars still lead the priming. |
| 5 | product | The broad external/formal trigger over-fires the disclosure note — if the longform supplement reads on nearly every formal email, the "leaned on supplemental source" note appears constantly and the user learns to ignore it. | Count how often the disclosure note fires across a sample of formal-email drafts; if it appears ~every time, the trigger is too broad — narrow it toward the "email.md formal coverage is thin" gate (Open Question 3). |
| 6 | product | voice-rewrite's unfixed email-scale craft clause ("Email-scale: only if long and formal") leaves a residual cross-register inconsistency — after this story, voice-rewrite still Strunk-edits long formal email-scale rewrites with longform exemptions while voice-email no longer does, so the suite is only partly consistent on the axis the epic names. | At acceptance, rewrite a long formal email via voice-rewrite and confirm whether it still applies the longform exemption list; verify a follow-up issue was filed (see verdict — this was left out of scope, not fixed). |
| 7 | technical | README's corrected craft-pass line drifts from the skill files — README:163 is changed to "email never gets a craft pass", but an incomplete deletion of voice-email Step 5 (a lingering strunk-rules.md reference) puts README and SKILL back out of sync. | Grep README and voice-email/SKILL.md for "craft pass" near "email"; confirm both agree email gets none, and that no `voice-doc/references/strunk-rules.md` read remains in voice-email. |
